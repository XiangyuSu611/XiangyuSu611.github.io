# Project Agent Notes

- For local previews, open the preview URL in Google Chrome by default.
- Use the in-app browser only for automated inspection, screenshots, or additional verification when needed.
- For this static site, start a local server from the repo root before previewing, for example `python3 -m http.server <port>`, then open `http://127.0.0.1:<port>/index.html` in Chrome.
